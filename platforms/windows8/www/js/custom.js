// this file, and each of its methods, should be overriden by an OS-specific custom.js from a merges/ directory

function openLink(link) {
	window.open(encodeURI(link)); 
}

function doCustomActions() {
	// intentionally left blank
}
